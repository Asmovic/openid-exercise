extends layout 

block content
    h1= title
    p Welcome to #{title}, #{user.nickname}!
    form(method='GET' action='/logout')
        button(type='submit') Logout
    form(method='GET' action='/profile')
        button.profile(type='submit') Profile
        